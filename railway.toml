[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/setup/healthz"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

# Require a persistent volume mounted at /data (Railway).
requiredMountPath = "/data"

[variables]
# NOTE: Railway injects PORT automatically.
# Do not set PORT here; Railway templates may not apply it reliably and hardcoding
# can cause domain routing mismatches.
OPENCLAW_STATE_DIR = "/data/.openclaw"
OPENCLAW_WORKSPACE_DIR = "/data/workspace"

# GitHub token for agent git operations (push, clone private repos, PRD Builder).
# The gh CLI reads GH_TOKEN; the wrapper also accepts GITHUB_TOKEN.
# Generate at: https://github.com/settings/tokens (needs 'repo' scope).
# GH_TOKEN = "${GH_TOKEN}"
